I was just curious to see whether esp-cam can be used for deep learning inference tasks. So I wrote a python script that fetches the video stream coming from the esp-cam, runs mobilenetv2 model in every 5 frames and writes the top preciction result and confidence over the frame. Turns out the overall system works quite good which is surprising.
